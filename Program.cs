using static ASmile.App.Entity.EntityFactory;
using static ASmile.App.Chapter.ChapterBuilder;
using static ASmile.App.Common.LanguageWrapper;
using static ASmile.App.Adj.Apply.AdjApply;
using static ASmile.App.consume.consumeApply;
using static ASmile.App.Comb.CompApply;
using ASmile.App.Entity;
using ASmile.App.Chapter;
using ASmile.App.Adj;
using ASmile.App.Adj.Apply;


//I saw an angel

Chapter<Intro>(() => {
    amidst(the<inkBlack, Swamps>().Embrace);
    each<Step>(_ =>
        a<whispered>(Invitation())
    ).to(_ =>
        meld(with(Earths(), gentle<Grace>()))
    );

    inThis<sylvan<Haven>>(() => i().reside());
    a<silent<Dweller>>().of(shadowed<Calm>())
    .Where(Rains().Touch.claims(Souls().untied()));

    within<Nights, Tapestry>(() => Shades().entwine());
    inThe<Heart>().ofThis<Obsidian, Sea>(_ => {
        emerges(a<gleam, Divine>());
    });

    a<solitary<Beacons>>().fleeting<Spark>();
    across<the<Ebony<Expanse>>>(_ => _.it.dances())
    .revealing(an<Angel>(inThe<Dark>()));
});

Chapter<A<Smile>>(() => {
    before<Her>(() => a<__Smile>().blooms());
    a<Balm>().to(myVery<Soul>());
    the<finest<Remedy>, myExistence>(graves);

    mothers<Deceit>();
    fathers<Blow>();
    they(() => pale(), insignificant)
    .beside<the<Radiance>>(She().bestows());

    Motivation().surges();
    Eyes().to(theSky());
    unyielding<Unity>();
    Road().uncharted();
    We()
        .indomitable()
        .inseparable();

    lost<adrift<inHer<Gaze>>>(i().find(_ => {
        Worlds(_).untamed();
        Galaxies(_).entwined();
    }));
    myself().dissolved(_inHer<Eyes>().divine());
});

Chapter<Enraptured<Reverie>>(() => {
    Eyes().of<Diamond>(_ =>
        Lights().Prism.aglow()
    );
    _Smile().that<elevates>()
        .to(Heaven => We().go());
    _= Voice() is A<Melody>;
    inEars().it(_ => _.sways());
    lost<entwined>(_ => _( inYour<captivating<Maze>>() ));

    each<Glance>(_ => a<Dance>());
    Emotions().take<Flight>();
    Your().Presence.a<Canvas>(_ => Colors().so<bright>());
    inWhispered<Breaths>(_ => theUniverse().sighs());
    _= But()!;
    inYour<loves<Gaze>>(() => my<Soul>(truly).flies());

    if (with<You>()) {
        i().am<complete>()
            .alive(inEvery<Way>());
    }
});